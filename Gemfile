source 'http://rubygems.org'

gem 'rake'
gem 'rspec' # for testing
gem 'rocco' # for generating documentation

# To generate documentation more quickly, you may want to install Pygments.
# `sudo easy_install Pygments`
# This is optional.  Without pygments, rocco will use a webservice instead.
